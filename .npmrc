# React Native's Metro bundler and native autolinking expect a flat node_modules
# layout. pnpm's default symlinked/isolated store breaks that resolution, so use
# a hoisted linker. This is the standard pnpm + Expo/React Native configuration.
node-linker=hoisted
