/**
 * Generated OpenAPI types for the FarmOS API. `schema.d.ts` is produced by
 * `pnpm api:generate`; consume these `paths`/`components` types when typing
 * requests and responses made through the HTTP client (see src/lib/http).
 */
export type { paths, components, operations, webhooks } from './schema';
